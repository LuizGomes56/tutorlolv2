[package]
name = "internal_comptime"
version = "0.1.0"
edition = "2024"

[dependencies]
bincode = "2.0.1"
phf = "0.12.1"
serde = { version = "1.0.219", features = ["derive"] }
shared_types = { path = "../shared_types" }
