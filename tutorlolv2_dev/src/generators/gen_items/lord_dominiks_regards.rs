use super::*;

impl Generator<ItemData>
    for LordDominiksRegards
{
    fn generate(
        self: Box<Self>,
    ) -> MayFail<ItemData> {
        /* No implementation */
        self.end()
    }
}
